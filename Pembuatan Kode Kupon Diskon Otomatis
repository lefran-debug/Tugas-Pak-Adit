const string1 = "diskon";
const string2 = "spesial50";

const kodeKupon = string1.concat(string2);
const kodeAkhir = kodeKupon.toUpperCase();

console.log(`Kode kupon: ${kodeAkhir}`);

Kode kupon: DISKONSPESIAL50
